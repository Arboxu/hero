<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical" android:layout_width="fill_parent"
	android:layout_height="fill_parent" android:padding="0.0dip"
	android:background="@android:color/background_light">
	<LinearLayout android:padding="10.0dip"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:id="@+id/title"
		android:background="#cdcfcd"
		android:layout_gravity="center_vertical"
		>
		<ImageView
			android:layout_gravity="center_vertical"
			android:id="@+id/icon"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_marginRight="10.0dip"
			android:src="@drawable/battle_death_icon_gray" />
		<TextView

			android:textSize="20sp"
			android:id="@+id/label"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_toRightOf="@id/icon"
			android:text="@string/str_selectserver"
			android:textColor="#1e1e1c"/>
	</LinearLayout>
	<ListView
		android:id="@+id/formcustomspinner_list"
		android:layout_width="fill_parent"
		android:layout_height="match_parent"
		android:layout_weight="1"
		android:cacheColorHint="@null"
		android:scrollbars="none"  
		android:divider="@android:drawable/divider_horizontal_bright"/><!--listview 的线为灰色 -->
</LinearLayout>
